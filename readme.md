Currently only supports Mac OSX, will work on linux with minimal modification and windows support will worked on in the coming months.

# Install pipenv
pip install pipenv
# Install deps
pipenv install
# Run Program
sudo pipenv run python ./main.py
